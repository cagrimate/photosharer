name: Twitter Botu Çalıştır

on:
  schedule:
    # 90 dakika aralıklarla çalıştır.
    - cron: "0 0,3,6,9,12,15,18,21 * * *"
    - cron: "30 1,4,7,10,13,16,19,22 * * *"
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Kodu çek
        uses: actions/checkout@v4
        with:
          persist-credentials: true   # push için önemli
          fetch-depth: 0              # tam history (bazı durumlar için iyi)

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Bağımlılıkları yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests tweepy python-dotenv google-genai schedule

      - name: Botu tek sefer çalıştır
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CONSUMER_KEY: ${{ secrets.CONSUMER_KEY }}
          CONSUMER_SECRET: ${{ secrets.CONSUMER_SECRET }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          ACCESS_TOKEN_SECRET: ${{ secrets.ACCESS_TOKEN_SECRET }}
        run: |
          python pexels_otomasyon.py once

      - name: paylasilan_idler.txt değiştiyse commit et
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # paylasilan_idler.txt değişmiş mi kontrol et
          if ! git diff --quiet -- paylasilan_idler.txt; then
            echo "🔄 ID dosyası güncellenmiş, commit ediliyor..."
            git add paylasilan_idler.txt
            git commit -m "Update shared Pexels IDs [skip ci]" || echo "Commit gerekmedi"
            git push
          else
            echo "ℹ️ ID dosyasında değişiklik yok, commit yok."
          fi
